Follow the steps to run the webapp, all dependancies have been included.

Open terminal / command line

Navigate to the root of roman fun


1. mvn package

2. java -jar target/dependency/webapp-runner.jar --port 8080 target/WebApp.war

3. open your browser, navigate to http://localhost:8080

